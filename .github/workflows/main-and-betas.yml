name: CI

on:
  push:
    branches:
      - main
    tags:
      - '*'

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        env_name: [dev, staging, beta, production]
    
    if: |
      ${{ matrix.env_name }} != 'beta' || 
      (${{ matrix.env_name }} == 'beta' && startsWith(github.ref, 'refs/tags/') && contains(github.ref, '-b'))

    steps:
      - name: Checkout code
        uses: actions/checkout@v2